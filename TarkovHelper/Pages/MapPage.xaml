<UserControl x:Class="TarkovHelper.Pages.MapPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:local="clr-namespace:TarkovHelper.Pages"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource BackgroundDarkBrush}"
             PreviewKeyDown="Page_PreviewKeyDown"
             Focusable="True">
    <UserControl.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:ProgressWidthConverter x:Key="ProgressWidthConverter"/>

        <!-- 툴바 버튼 스타일 -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource BackgroundDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 툴바 섹션 스타일 -->
        <Style x:Key="ToolbarSectionStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>

        <!-- 설정 탭 스타일 -->
        <Style x:Key="SettingsTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource BackgroundDarkBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Toolbar - 최소화된 헤더 (방안 A) -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}"
                Padding="12,6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="1" Opacity="0.25" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Map Selector -->
                <Border Grid.Column="0" Style="{StaticResource ToolbarSectionStyle}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📍" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ComboBox x:Name="MapSelector" Width="140" Height="26"
                                  SelectionChanged="MapSelector_SelectionChanged"
                                  DisplayMemberPath="DisplayName"/>
                    </StackPanel>
                </Border>

                <!-- Floor Selector -->
                <Border x:Name="FloorSelectorPanel" Grid.Column="1" Style="{StaticResource ToolbarSectionStyle}"
                        Margin="8,0,0,0" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <ComboBox x:Name="FloorSelector" Width="90" Height="26"
                                  SelectionChanged="FloorSelector_SelectionChanged"/>
                        <TextBlock x:Name="TxtFloorHotkeys" Text="0-5" FontSize="9"
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0"
                                   VerticalAlignment="Center" Opacity="0.6"/>
                    </StackPanel>
                </Border>

                <!-- Spacer -->
                <TextBlock x:Name="TxtFloorLabel" Grid.Column="2" Visibility="Collapsed"/>

                <!-- Quest Panel Toggle -->
                <Button x:Name="BtnToggleQuestPanel" Grid.Column="3" Style="{StaticResource ToolbarButtonStyle}"
                        Padding="10,6" Margin="0,0,8,0"
                        Click="BtnToggleDrawer_Click" ToolTip="퀘스트 패널 열기/닫기 (Q)">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" FontSize="12" VerticalAlignment="Center"/>
                        <TextBlock x:Name="HeaderQuestLabel" Text="Quest" FontSize="11" Margin="6,0,0,0"
                                   Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock x:Name="HeaderQuestCount" Text="" FontSize="10"
                                   Foreground="{StaticResource AccentBrush}" Margin="4,0,0,0"
                                   VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <!-- Settings Button -->
                <Button x:Name="BtnSettings" Grid.Column="4" Style="{StaticResource ToolbarButtonStyle}"
                        Width="36" Height="32" FontSize="16" Content="⚙"
                        Click="BtnSettings_Click" ToolTip="설정 (레이어, 마커 크기, 트래커)"/>
            </Grid>
        </Border>

        <!-- Main Content with Drawer -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="QuestDrawerColumn" Width="0"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Quest Drawer (Left) - 새 UI -->
            <Border x:Name="QuestDrawerPanel" Grid.Column="0"
                    Background="{StaticResource BackgroundMediumBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,1,0"
                    Visibility="Collapsed"
                    KeyDown="QuestDrawer_KeyDown" Focusable="True">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.4" Direction="0"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 0: 컴팩트 헤더 -->
                    <Border Grid.Row="0" Padding="12,8" Background="{StaticResource BackgroundLightBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 제목 + 카운트 -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="DrawerTitleText" Text="Quest" FontWeight="SemiBold" FontSize="13"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                                <Border Background="{StaticResource AccentBrush}" CornerRadius="8" Padding="6,1" Margin="8,0,0,0">
                                    <TextBlock x:Name="QuestCountText" Text="0" FontSize="10" FontWeight="Medium"
                                               Foreground="White"/>
                                </Border>
                            </StackPanel>

                            <!-- 헤더 버튼들 -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button x:Name="BtnDrawerHelp" Content="?" Width="22" Height="22"
                                        Style="{StaticResource ToolbarButtonStyle}" FontSize="10"
                                        Click="BtnDrawerHelp_Click" ToolTip="단축키 도움말" Margin="0,0,4,0"/>
                                <Button Content="⚙" Width="22" Height="22"
                                        Style="{StaticResource ToolbarButtonStyle}" FontSize="10"
                                        Click="BtnDrawerOptions_Click" ToolTip="표시 옵션" Margin="0,0,4,0"/>
                                <Button Content="✕" Width="22" Height="22"
                                        Style="{StaticResource ToolbarButtonStyle}" FontSize="10"
                                        Click="BtnCloseDrawer_Click" ToolTip="닫기 (Q)"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Row 1: 검색 + 인라인 필터 -->
                    <Border Grid.Row="1" Padding="10,8" Background="{StaticResource BackgroundMediumBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <StackPanel>
                            <!-- 검색창 -->
                            <Grid Margin="0,0,0,8">
                                <TextBox x:Name="SearchBox" Background="{StaticResource BackgroundDarkBrush}"
                                         Foreground="{StaticResource TextPrimaryBrush}"
                                         BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                         Padding="8,6" FontSize="11" TextChanged="SearchBox_TextChanged"/>
                                <TextBlock x:Name="SearchPlaceholderText" Text="🔍 Search..." FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Padding="8,7" IsHitTestVisible="False" Opacity="0.6"
                                           Visibility="{Binding Text, ElementName=SearchBox, Converter={StaticResource StringToVisibilityConverter}}"/>
                            </Grid>

                            <!-- 인라인 필터 -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 필터 토글 -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <CheckBox x:Name="ChkIncompleteOnly" Foreground="{StaticResource TextSecondaryBrush}"
                                              Checked="DrawerFilter_Changed" Unchecked="DrawerFilter_Changed">
                                        <TextBlock x:Name="FilterIncompleteText" Text="Incomplete" FontSize="10"/>
                                    </CheckBox>
                                    <CheckBox x:Name="ChkCurrentMapOnly" IsChecked="True" Margin="12,0,0,0"
                                              Foreground="{StaticResource TextSecondaryBrush}"
                                              Checked="DrawerFilter_Changed" Unchecked="DrawerFilter_Changed">
                                        <TextBlock x:Name="FilterCurrentMapText" Text="Current Map" FontSize="10"/>
                                    </CheckBox>
                                </StackPanel>

                                <!-- 정렬 -->
                                <ComboBox Grid.Column="1" x:Name="CboDrawerSort" Width="70" FontSize="10"
                                          Background="{StaticResource BackgroundDarkBrush}"
                                          SelectionChanged="SortOption_Changed" SelectedIndex="0">
                                    <ComboBoxItem x:Name="SortByNameItem" Content="Name" Tag="name"/>
                                    <ComboBoxItem x:Name="SortByProgressItem" Content="Progress" Tag="progress"/>
                                    <ComboBoxItem x:Name="SortByCountItem" Content="Count" Tag="count"/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Row 2: 퀘스트 목록 (새 카드 스타일) -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="0,4">
                        <Grid>
                            <!-- 빈 상태 -->
                            <StackPanel x:Name="EmptyStatePanel" Visibility="Collapsed"
                                        VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
                                <TextBlock Text="📋" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8" Opacity="0.4"/>
                                <TextBlock x:Name="EmptyStateTitle" Text="No quests to display" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock x:Name="EmptyStateSubtitle" Text="Try adjusting filters" FontSize="10"
                                           Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"
                                           Margin="0,4,0,0" Opacity="0.6"/>
                            </StackPanel>

                            <!-- 퀘스트 카드 목록 -->
                            <ItemsControl x:Name="QuestObjectivesList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <!-- 퀘스트 카드 -->
                                        <Border Margin="8,4" CornerRadius="6"
                                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                            <Setter Property="Opacity" Value="0.6"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                                            <Setter Property="Background" Value="#3D3D20"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Border.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="모두 완료" Click="QuestContextMenu_MarkAllComplete"/>
                                                    <MenuItem Header="모두 미완료" Click="QuestContextMenu_MarkAllIncomplete"/>
                                                    <Separator/>
                                                    <MenuItem Header="맵에서 숨기기" Click="QuestContextMenu_HideFromMap"/>
                                                </ContextMenu>
                                            </Border.ContextMenu>

                                            <StackPanel>
                                                <!-- 카드 헤더 -->
                                                <Border Padding="10,8" Cursor="Hand" MouseLeftButtonDown="QuestHeader_Click"
                                                        Background="Transparent">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>

                                                        <!-- 첫째 줄: 퀘스트명 + 토글 -->
                                                        <Grid Grid.Row="0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <!-- 펼침 아이콘 -->
                                                            <TextBlock Grid.Column="0" FontSize="9" Width="14"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       VerticalAlignment="Center">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Text" Value="▼"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                                                                                <Setter Property="Text" Value="▶"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>

                                                            <!-- 퀘스트명 -->
                                                            <TextBlock Grid.Column="1" Text="{Binding QuestName}" FontSize="12" FontWeight="Medium"
                                                                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>

                                                            <!-- 맵 표시 토글 -->
                                                            <Button Grid.Column="2" Width="20" Height="20" Margin="4,0"
                                                                    Style="{StaticResource ToolbarButtonStyle}" FontSize="10"
                                                                    Click="QuestVisibilityToggle_Click"
                                                                    ToolTip="맵에 표시/숨김">
                                                                <TextBlock>
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Text" Value="👁"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                                                                    <Setter Property="Text" Value="👁‍🗨"/>
                                                                                    <Setter Property="Opacity" Value="0.4"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </Button>

                                                            <!-- 진행률 텍스트 -->
                                                            <TextBlock Grid.Column="3" Text="{Binding ProgressText}" FontSize="11" FontWeight="Medium"
                                                                       VerticalAlignment="Center" MinWidth="35" TextAlignment="Right">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </Grid>

                                                        <!-- 둘째 줄: 진행률 바 -->
                                                        <Grid Grid.Row="1" Height="4" Margin="14,6,0,0">
                                                            <Border Background="{StaticResource BackgroundDarkBrush}" CornerRadius="2"/>
                                                            <Border CornerRadius="2" HorizontalAlignment="Left"
                                                                    Width="{Binding ProgressPercent, Converter={StaticResource ProgressWidthConverter}}">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                            </Border>
                                                        </Grid>
                                                    </Grid>
                                                </Border>

                                                <!-- 목표 리스트 (트리 스타일) -->
                                                <ItemsControl ItemsSource="{Binding Objectives}">
                                                    <ItemsControl.Style>
                                                        <Style TargetType="ItemsControl">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ItemsControl.Style>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Padding="10,5" Cursor="Hand"
                                                                    MouseLeftButtonDown="QuestDrawerItem_Click"
                                                                    MouseRightButtonDown="QuestDrawerItem_RightClick"
                                                                    MouseEnter="QuestDrawerItem_MouseEnter"
                                                                    MouseLeave="QuestDrawerItem_MouseLeave"
                                                                    BorderBrush="{StaticResource BorderBrush}"
                                                                    BorderThickness="0,1,0,0">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="Transparent"/>
                                                                        <Style.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
                                                                            </Trigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>

                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                    </Grid.ColumnDefinitions>

                                                                    <!-- 트리 라인 + 체크 -->
                                                                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="4,0,0,0">
                                                                        <TextBlock Text="├" FontSize="10" Foreground="{StaticResource BorderBrush}"
                                                                                   VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                                        <TextBlock FontSize="11" VerticalAlignment="Center" Width="14">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Text" Value="○"/>
                                                                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                            <Setter Property="Text" Value="✓"/>
                                                                                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </StackPanel>

                                                                    <!-- 타입 아이콘 -->
                                                                    <TextBlock Grid.Column="1" Text="{Binding TypeIcon}" FontSize="11"
                                                                               Margin="4,0,6,0" VerticalAlignment="Center"
                                                                               ToolTip="{Binding Objective.Type}"/>

                                                                    <!-- 설명 -->
                                                                    <TextBlock Grid.Column="2" Text="{Binding DescriptionDisplay}" FontSize="11"
                                                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                        <Setter Property="Opacity" Value="0.5"/>
                                                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>

                                                                    <!-- 맵 이동 버튼 -->
                                                                    <Button Grid.Column="3" Content="›" Width="18" Height="18"
                                                                            Style="{StaticResource ToolbarButtonStyle}" FontSize="12" FontWeight="Bold"
                                                                            Click="ObjectiveGoToMap_Click" ToolTip="맵에서 보기"
                                                                            Visibility="{Binding HasLocation, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>

                    <!-- Row 3: 키보드 힌트 (토글 가능) -->
                    <Border x:Name="DrawerHelpPanel" Grid.Row="3" Background="{StaticResource BackgroundDarkBrush}"
                            Padding="10,6" Visibility="Collapsed"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock x:Name="HelpOpenClose" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}">
                                    <Run Text="Q" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                    <Run x:Name="HelpOpenCloseText" Text=" Open/Close"/>
                                </TextBlock>
                                <TextBlock x:Name="HelpMove" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0">
                                    <Run Text="↑↓" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                    <Run x:Name="HelpMoveText" Text=" Move"/>
                                </TextBlock>
                                <TextBlock x:Name="HelpSelect" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0">
                                    <Run Text="Enter" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                    <Run x:Name="HelpSelectText" Text=" Select"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                                <TextBlock x:Name="HelpClick" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}">
                                    <Run x:Name="HelpClickKey" Text="Click" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                    <Run x:Name="HelpClickText" Text=" Go to Map"/>
                                </TextBlock>
                                <TextBlock x:Name="HelpRightClick" FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0">
                                    <Run x:Name="HelpRightClickKey" Text="Right-click" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                    <Run x:Name="HelpRightClickText" Text=" Toggle"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Resize Grip -->
                    <Border Grid.RowSpan="4" Width="5" HorizontalAlignment="Right" Background="Transparent"
                            Cursor="SizeWE" MouseLeftButtonDown="DrawerResizeGrip_MouseDown"
                            MouseMove="DrawerResizeGrip_MouseMove" MouseLeftButtonUp="DrawerResizeGrip_MouseUp">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </Grid>
            </Border>

            <!-- Drawer Toggle Button -->
            <Border Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0"
                    Panel.ZIndex="10" CornerRadius="0,6,6,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="2" Opacity="0.4" Direction="0"/>
                </Border.Effect>
                <Button x:Name="BtnToggleDrawer" Width="28" Height="90"
                        Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,1,1"
                        Click="BtnToggleDrawer_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="0,6,6,0">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock x:Name="arrow" Text="◀" FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="Q" FontSize="9" Margin="0,4,0,0"
                                               Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                    <Setter TargetName="arrow" Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Border>

            <!-- Map Viewer -->
            <Grid Grid.Column="1" x:Name="MapViewerGrid" Background="{StaticResource BackgroundDarkBrush}"
                  ClipToBounds="True"
                  MouseLeftButtonDown="MapViewer_MouseLeftButtonDown"
                  MouseLeftButtonUp="MapViewer_MouseLeftButtonUp"
                  MouseMove="MapViewer_MouseMove"
                  MouseWheel="MapViewer_MouseWheel">

                <Canvas x:Name="MapCanvas" RenderTransformOrigin="0,0">
                    <Canvas.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="MapScale" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="MapTranslate" X="0" Y="0"/>
                        </TransformGroup>
                    </Canvas.RenderTransform>

                    <!-- SVG Map Background -->
                    <svgc:SvgViewbox x:Name="MapSvg" Stretch="None" Canvas.Left="0" Canvas.Top="0"/>

                    <!-- Markers Layer -->
                    <Canvas x:Name="MarkersCanvas"/>

                    <!-- Transit Layer -->
                    <Canvas x:Name="TransitCanvas"/>

                    <!-- Quest Markers Layer -->
                    <Canvas x:Name="QuestMarkersCanvas"/>

                    <!-- Trail Layer (player movement path) -->
                    <Canvas x:Name="TrailCanvas"/>

                    <!-- Player Position Marker -->
                    <Canvas x:Name="PlayerMarkerCanvas"/>
                </Canvas>

                <!-- Loading Indicator -->
                <Border x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Width="40" Height="40" CornerRadius="20" Background="{StaticResource AccentBrush}">
                            <Border.RenderTransform>
                                <RotateTransform x:Name="LoadingRotation" CenterX="20" CenterY="20"/>
                            </Border.RenderTransform>
                            <TextBlock Text="⟳" FontSize="24" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock x:Name="LoadingText" Text="Loading map..." FontSize="12" Margin="0,12,0,0"
                                   Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Legend Popup -->
                <Border x:Name="LegendPopup" Visibility="Collapsed"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Margin="0,10,10,0" Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="6" Padding="14">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="LegendTitle" Text="Map Legend" FontWeight="SemiBold" FontSize="13"
                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Width="14" Height="14" CornerRadius="3" Background="{StaticResource SuccessBrush}"/>
                            <TextBlock x:Name="LegendExtract" Text="Extract" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Width="14" Height="14" CornerRadius="3" Background="#FF9800"/>
                            <TextBlock x:Name="LegendTransit" Text="Transit Point" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Width="14" Height="14" CornerRadius="7" Background="{StaticResource WarningBrush}"/>
                            <TextBlock x:Name="LegendQuestObjective" Text="Quest Objective" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,10"/>

                        <TextBlock x:Name="LegendQuestType" Text="Quest Type" FontWeight="SemiBold" FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="📍" FontSize="12" Width="20"/>
                            <TextBlock x:Name="LegendVisit" Text="Visit" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="🎯" FontSize="12" Width="20"/>
                            <TextBlock x:Name="LegendMark" Text="Mark" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="📦" FontSize="12" Width="20"/>
                            <TextBlock x:Name="LegendPlantItem" Text="Plant Item" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="🚪" FontSize="12" Width="20"/>
                            <TextBlock x:Name="LegendExtractType" Text="Extract" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="💀" FontSize="12" Width="20"/>
                            <TextBlock x:Name="LegendKill" Text="Kill" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Button x:Name="LegendCloseBtn" Content="Close" Margin="0,12,0,0" Padding="12,6" FontSize="10"
                                Style="{StaticResource ToolbarButtonStyle}" Click="BtnCloseLegend_Click"/>
                    </StackPanel>
                </Border>

                <!-- Quest Type Filter Popup -->
                <Border x:Name="QuestFilterPopup" Visibility="Collapsed"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Margin="0,10,100,0" Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="6" Padding="14">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock x:Name="FilterPopupTitle" Text="Quest Type Filter" FontWeight="SemiBold" FontSize="13"
                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                        <CheckBox x:Name="ChkFilterVisit" IsChecked="True" Margin="0,4"
                                  Checked="QuestTypeFilter_Changed" Unchecked="QuestTypeFilter_Changed">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" CornerRadius="2" Background="#2196F3" Margin="0,0,8,0"/>
                                <TextBlock x:Name="FilterVisitText" Text="Visit" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkFilterMark" IsChecked="True" Margin="0,4"
                                  Checked="QuestTypeFilter_Changed" Unchecked="QuestTypeFilter_Changed">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" CornerRadius="2" Background="#4CAF50" Margin="0,0,8,0"/>
                                <TextBlock x:Name="FilterMarkText" Text="Mark" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkFilterPlant" IsChecked="True" Margin="0,4"
                                  Checked="QuestTypeFilter_Changed" Unchecked="QuestTypeFilter_Changed">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" CornerRadius="2" Background="#FF9800" Margin="0,0,8,0"/>
                                <TextBlock x:Name="FilterPlantText" Text="Plant Item" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkFilterExtract" IsChecked="True" Margin="0,4"
                                  Checked="QuestTypeFilter_Changed" Unchecked="QuestTypeFilter_Changed">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" CornerRadius="2" Background="#F44336" Margin="0,0,8,0"/>
                                <TextBlock x:Name="FilterExtractText" Text="Extract" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkFilterFind" IsChecked="True" Margin="0,4"
                                  Checked="QuestTypeFilter_Changed" Unchecked="QuestTypeFilter_Changed">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" CornerRadius="2" Background="#FFEB3B" Margin="0,0,8,0"/>
                                <TextBlock x:Name="FilterFindText" Text="Find Item" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkFilterKill" IsChecked="True" Margin="0,4"
                                  Checked="QuestTypeFilter_Changed" Unchecked="QuestTypeFilter_Changed">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" CornerRadius="2" Background="#9C27B0" Margin="0,0,8,0"/>
                                <TextBlock x:Name="FilterKillText" Text="Kill" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkFilterOther" IsChecked="True" Margin="0,4"
                                  Checked="QuestTypeFilter_Changed" Unchecked="QuestTypeFilter_Changed">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="12" Height="12" CornerRadius="2" Background="#FFC107" Margin="0,0,8,0"/>
                                <TextBlock x:Name="FilterOtherText" Text="Other" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </CheckBox>

                        <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,10"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button x:Name="BtnSelectAll" Content="Select All" Padding="8,4" FontSize="10" Margin="0,0,8,0"
                                    Style="{StaticResource ToolbarButtonStyle}" Click="BtnSelectAllFilters_Click"/>
                            <Button x:Name="BtnDeselectAll" Content="Deselect All" Padding="8,4" FontSize="10"
                                    Style="{StaticResource ToolbarButtonStyle}" Click="BtnClearAllFilters_Click"/>
                        </StackPanel>

                        <Button x:Name="FilterCloseBtn" Content="Close" Margin="0,10,0,0" Padding="12,6" FontSize="10"
                                Style="{StaticResource ToolbarButtonStyle}" Click="BtnCloseQuestFilter_Click"/>
                    </StackPanel>
                </Border>

                <!-- Minimap Overlay -->
                <Border x:Name="MinimapContainer" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                        Margin="0,0,10,10" Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="6" Padding="3">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.5"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Minimap Header -->
                        <Border Grid.Row="0" Background="{StaticResource BackgroundLightBrush}"
                                CornerRadius="4,4,0,0" Padding="6,3" Cursor="Hand"
                                MouseLeftButtonDown="MinimapHeader_Click">
                            <Grid>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="MinimapLabel" Text="Minimap" FontSize="10" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"/>
                                    <TextBlock x:Name="MinimapMapName" Text="" FontSize="9"
                                               Foreground="{StaticResource AccentBrush}"
                                               VerticalAlignment="Center" Margin="6,0,0,0"/>
                                </StackPanel>
                                <TextBlock x:Name="MinimapToggleIcon" Text="▼" FontSize="8"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Minimap Content - 동적 크기 -->
                        <Border x:Name="MinimapContent" Grid.Row="1"
                                MinWidth="120" MaxWidth="280"
                                Background="{StaticResource BackgroundDarkBrush}"
                                CornerRadius="0,0,4,4" ClipToBounds="True"
                                MouseLeftButtonDown="Minimap_MouseLeftButtonDown"
                                MouseMove="Minimap_MouseMove"
                                Cursor="Hand">
                            <Grid x:Name="MinimapGrid">
                                <!-- Minimap Image (scaled down SVG or thumbnail) -->
                                <Image x:Name="MinimapImage" Stretch="Fill"/>

                                <!-- Minimap Marker Layer - 마커 표시 -->
                                <Canvas x:Name="MinimapMarkerCanvas" ClipToBounds="True"/>

                                <!-- Viewport Rectangle -->
                                <Canvas x:Name="MinimapViewportCanvas" ClipToBounds="True">
                                    <Rectangle x:Name="MinimapViewport"
                                               Fill="#20FFFFFF"
                                               Stroke="{StaticResource AccentBrush}"
                                               StrokeThickness="1.5"
                                               RadiusX="2" RadiusY="2"/>
                                </Canvas>

                                <!-- Player Position on Minimap -->
                                <Canvas x:Name="MinimapPlayerCanvas"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- Keyboard Shortcuts Hint (맵 영역) -->
                <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="40,0,0,10"
                        Background="#90000000" CornerRadius="4" Padding="10,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}">
                            <Run x:Name="MapHintScrollKey" Text="Scroll" Foreground="{StaticResource AccentBrush}"/>
                            <Run x:Name="MapHintZoomText" Text=" Zoom"/>
                        </TextBlock>
                        <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0">
                            <Run x:Name="MapHintDragKey" Text="Drag" Foreground="{StaticResource AccentBrush}"/>
                            <Run x:Name="MapHintMoveText" Text=" Move"/>
                        </TextBlock>
                        <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0">
                            <Run Text="R" Foreground="{StaticResource AccentBrush}"/>
                            <Run x:Name="MapHintResetText" Text=" Reset"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Vertical Zoom Controls (우측 세로 배치) -->
                <Border HorizontalAlignment="Right" VerticalAlignment="Center"
                        Margin="0,0,10,0" Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="6" Padding="4">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="2" Opacity="0.4"/>
                    </Border.Effect>
                    <StackPanel Orientation="Vertical">
                        <Button x:Name="BtnZoomIn" Style="{StaticResource ToolbarButtonStyle}"
                                Content="+" Width="32" Height="32" FontSize="18" FontWeight="Bold"
                                Click="BtnZoomIn_Click" ToolTip="확대 (Scroll Up)"/>
                        <Border Background="{StaticResource BackgroundDarkBrush}" CornerRadius="4" Margin="0,4" Padding="6,8">
                            <TextBlock x:Name="ZoomText" Text="100%" Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="10" FontWeight="SemiBold" TextAlignment="Center"
                                       Width="20"/>
                        </Border>
                        <Button x:Name="BtnZoomOut" Style="{StaticResource ToolbarButtonStyle}"
                                Content="−" Width="32" Height="32" FontSize="18" FontWeight="Bold"
                                Click="BtnZoomOut_Click" ToolTip="축소 (Scroll Down)"/>
                        <Rectangle Height="1" Fill="{StaticResource BorderBrush}" Margin="4,6"/>
                        <Button x:Name="BtnResetView" Style="{StaticResource ToolbarButtonStyle}"
                                Content="⟲" Width="32" Height="32" FontSize="16"
                                Click="BtnResetView_Click" ToolTip="뷰 초기화 (R)"/>
                    </StackPanel>
                </Border>

                <!-- Settings Popup - 탭 기반 설정 패널 -->
                <Border x:Name="SettingsPopup" Visibility="Collapsed"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Margin="0,10,60,0" Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="8" Width="320" MaxHeight="500">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="4" Opacity="0.5"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Border Grid.Row="0" Background="{StaticResource BackgroundLightBrush}"
                                CornerRadius="8,8,0,0" Padding="14,10">
                            <Grid>
                                <TextBlock x:Name="SettingsHeaderText" Text="⚙ Settings" FontWeight="SemiBold" FontSize="14"
                                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <Button Content="✕" HorizontalAlignment="Right" Width="24" Height="24"
                                        Style="{StaticResource ToolbarButtonStyle}" Click="BtnCloseSettings_Click" FontSize="11"/>
                            </Grid>
                        </Border>

                        <!-- Tab Headers -->
                        <Border Grid.Row="1" Background="{StaticResource BackgroundDarkBrush}" Padding="4">
                            <UniformGrid Rows="1" Columns="4">
                                <RadioButton x:Name="TabDisplay" Content="Display" GroupName="SettingsTabs"
                                             IsChecked="True" Click="SettingsTab_Click" Tag="Display"
                                             Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="TabMarker" Content="Marker" GroupName="SettingsTabs"
                                             Click="SettingsTab_Click" Tag="Marker"
                                             Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="TabTracker" Content="Tracker" GroupName="SettingsTabs"
                                             Click="SettingsTab_Click" Tag="Tracker"
                                             Style="{StaticResource SettingsTabStyle}"/>
                                <RadioButton x:Name="TabShortcuts" Content="Shortcuts" GroupName="SettingsTabs"
                                             Click="SettingsTab_Click" Tag="Shortcuts"
                                             Style="{StaticResource SettingsTabStyle}"/>
                            </UniformGrid>
                        </Border>

                        <!-- Tab Content -->
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="12,10">
                            <Grid>
                                <!-- [표시] 탭 -->
                                <StackPanel x:Name="PanelDisplay" Visibility="Visible">
                                    <!-- 레이어 표시 -->
                                    <TextBlock x:Name="SettingsLayersLabel" Text="Layers" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <!-- 탈출구 토글 -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="10" Height="10" CornerRadius="2" Background="{StaticResource SuccessBrush}" Margin="0,0,8,0"/>
                                            <TextBlock x:Name="SettingsExtractLabel" Text="Extract" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="E" FontSize="9" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="4,2"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <CheckBox x:Name="ChkShowMarkers" Grid.Column="2" IsChecked="True"
                                                  Checked="LayerVisibility_Changed" Unchecked="LayerVisibility_Changed"/>
                                    </Grid>

                                    <!-- 환승 토글 -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="10" Height="10" CornerRadius="2" Background="#FF9800" Margin="0,0,8,0"/>
                                            <TextBlock x:Name="SettingsTransitLabel" Text="Transit Point" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="T" FontSize="9" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="4,2"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <CheckBox x:Name="ChkShowTransit" Grid.Column="2" IsChecked="True"
                                                  Checked="LayerVisibility_Changed" Unchecked="LayerVisibility_Changed"/>
                                    </Grid>

                                    <!-- 퀘스트 토글 -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="10" Height="10" CornerRadius="5" Background="{StaticResource WarningBrush}" Margin="0,0,8,0"/>
                                            <TextBlock x:Name="SettingsQuestObjLabel" Text="Quest Objective" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="Q" FontSize="9" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="4,2"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <CheckBox x:Name="ChkShowQuests" Grid.Column="2" IsChecked="True"
                                                  Checked="LayerVisibility_Changed" Unchecked="LayerVisibility_Changed"/>
                                    </Grid>

                                    <!-- 이동 경로 토글 -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Width="10" Height="10" CornerRadius="5" Background="#2196F3" Margin="0,0,8,0"/>
                                            <TextBlock x:Name="SettingsTrailLabel" Text="Trail" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        </StackPanel>
                                        <CheckBox x:Name="ChkShowTrail" Grid.Column="1" IsChecked="True"
                                                  Checked="TrailVisibility_Changed" Unchecked="TrailVisibility_Changed"/>
                                    </Grid>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,6,0,10"/>

                                    <!-- 미니맵 설정 -->
                                    <TextBlock x:Name="SettingsMinimapLabel" Text="Minimap" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="SettingsShowMinimapLabel" Grid.Column="0" Text="Show Minimap" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" VerticalAlignment="Center"/>
                                        <CheckBox x:Name="ChkShowMinimap" Grid.Column="1" IsChecked="True"
                                                  Checked="MinimapVisibility_Changed" Unchecked="MinimapVisibility_Changed"/>
                                    </Grid>

                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock x:Name="SettingsMinimapSizeLabel" Text="Minimap Size" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" Margin="0,0,0,6"/>
                                        <StackPanel Orientation="Horizontal">
                                            <RadioButton x:Name="MinimapSizeS" Content="S" GroupName="MinimapSize"
                                                         Click="MinimapSize_Changed" Tag="Small" Margin="0,0,8,0"
                                                         Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <RadioButton x:Name="MinimapSizeM" Content="M" GroupName="MinimapSize"
                                                         Click="MinimapSize_Changed" Tag="Medium" IsChecked="True" Margin="0,0,8,0"
                                                         Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <RadioButton x:Name="MinimapSizeL" Content="L" GroupName="MinimapSize"
                                                         Click="MinimapSize_Changed" Tag="Large"
                                                         Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,6,0,10"/>

                                    <!-- 퀵 액션 -->
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button x:Name="BtnQuestFilter" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="12,8" Margin="0,0,8,0"
                                                Click="BtnQuestFilter_Click" ToolTip="Quest Type Filter">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🎯" FontSize="12"/>
                                                <TextBlock x:Name="SettingsQuestFilterLabel" Text="Quest Filter" FontSize="10" Margin="6,0,0,0"
                                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </Button>
                                        <Button x:Name="BtnLegend" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="12,8"
                                                Click="BtnLegend_Click" ToolTip="View Map Legend">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="?" FontSize="14" FontWeight="Bold"/>
                                                <TextBlock x:Name="SettingsLegendLabel" Text="Legend" FontSize="10" Margin="6,0,0,0"
                                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>

                                <!-- [마커] 탭 -->
                                <StackPanel x:Name="PanelMarker" Visibility="Collapsed">
                                    <!-- 마커 크기 -->
                                    <TextBlock x:Name="SettingsMarkerSizeLabel" Text="Marker Size" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <!-- 프리셋 버튼 -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8" HorizontalAlignment="Center">
                                        <Button Content="0.5x" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,4" Margin="0,0,4,0" FontSize="10"
                                                Click="MarkerScalePreset_Click" Tag="0.5"/>
                                        <Button Content="1.0x" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,4" Margin="0,0,4,0" FontSize="10"
                                                Click="MarkerScalePreset_Click" Tag="1.0"/>
                                        <Button Content="1.5x" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,4" Margin="0,0,4,0" FontSize="10"
                                                Click="MarkerScalePreset_Click" Tag="1.5"/>
                                        <Button Content="2.0x" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,4" FontSize="10"
                                                Click="MarkerScalePreset_Click" Tag="2.0"/>
                                    </StackPanel>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Slider x:Name="MarkerScaleSlider" Grid.Column="0" Minimum="0.5" Maximum="2.0" Value="1.0"
                                                VerticalAlignment="Center" SmallChange="0.1" LargeChange="0.25"
                                                ValueChanged="MarkerScaleSlider_ValueChanged"/>
                                        <TextBlock x:Name="MarkerScaleText" Grid.Column="1" Text="1.0x" FontSize="12" Width="40"
                                                   Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"
                                                   VerticalAlignment="Center" Margin="8,0,0,0" TextAlignment="Right"/>
                                    </Grid>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,6,0,10"/>

                                    <!-- 마커 투명도 -->
                                    <TextBlock x:Name="SettingsMarkerOpacityLabel" Text="Marker Opacity" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Slider x:Name="MarkerOpacitySlider" Grid.Column="0" Minimum="0.3" Maximum="1.0" Value="1.0"
                                                VerticalAlignment="Center" SmallChange="0.1" LargeChange="0.2"
                                                ValueChanged="MarkerOpacitySlider_ValueChanged"/>
                                        <TextBlock x:Name="MarkerOpacityText" Grid.Column="1" Text="100%" FontSize="12" Width="40"
                                                   Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"
                                                   VerticalAlignment="Center" Margin="8,0,0,0" TextAlignment="Right"/>
                                    </Grid>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,6,0,10"/>

                                    <!-- 퀘스트 표시 옵션 -->
                                    <TextBlock x:Name="SettingsQuestDisplayLabel" Text="Quest Display" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="SettingsAutoHideCompletedLabel" Grid.Column="0" Text="Auto-hide Completed" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" VerticalAlignment="Center"/>
                                        <CheckBox x:Name="ChkAutoHideCompleted" Grid.Column="1" IsChecked="False"
                                                  Checked="AutoHideCompleted_Changed" Unchecked="AutoHideCompleted_Changed"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="SettingsFadeCompletedLabel" Grid.Column="0" Text="Fade Completed" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" VerticalAlignment="Center"/>
                                        <CheckBox x:Name="ChkFadeCompleted" Grid.Column="1" IsChecked="True"
                                                  Checked="FadeCompleted_Changed" Unchecked="FadeCompleted_Changed"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="SettingsShowLabelsLabel" Grid.Column="0" Text="Show Labels" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" VerticalAlignment="Center"/>
                                        <CheckBox x:Name="ChkShowLabels" Grid.Column="1" IsChecked="True"
                                                  Checked="ShowLabels_Changed" Unchecked="ShowLabels_Changed"/>
                                    </Grid>
                                </StackPanel>

                                <!-- [트래커] 탭 -->
                                <StackPanel x:Name="PanelTracker" Visibility="Collapsed">
                                    <!-- 트래커 상태 -->
                                    <TextBlock x:Name="SettingsTrackerStatusLabel" Text="Tracker Status" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Border Background="{StaticResource BackgroundLightBrush}" CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Border x:Name="TrackingStatusBorder" Grid.Column="0" Width="12" Height="12" CornerRadius="6"
                                                    Background="Gray" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock x:Name="TrackingStatusText" Text="Waiting" FontSize="12"
                                                           Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                                                <TextBlock x:Name="TrackingFolderText" Text="No folder selected" FontSize="10"
                                                           Foreground="{StaticResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- 트래커 컨트롤 -->
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                                        <Button x:Name="BtnSelectFolder" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,6" Margin="0,0,4,0"
                                                Click="BtnSelectFolder_Click" ToolTip="Select screenshot folder">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📁" FontSize="14"/>
                                                <TextBlock x:Name="BtnSelectFolderText" Text="Folder" FontSize="10" Margin="4,0,0,0"
                                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </Button>
                                        <Button x:Name="BtnOpenFolder" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,6" Margin="0,0,4,0"
                                                Click="BtnOpenFolder_Click" ToolTip="Open folder">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📂" FontSize="13"/>
                                                <TextBlock x:Name="BtnOpenFolderText" Text="Open" FontSize="10" Margin="3,0,0,0"
                                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </Button>
                                        <Button x:Name="BtnToggleTracking" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,6" Margin="0,0,4,0"
                                                Click="BtnToggleTracking_Click" ToolTip="Start/Stop tracking">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock x:Name="TrackingButtonIcon" Text="▶" FontSize="11"/>
                                                <TextBlock x:Name="TrackingButtonText" Text="Start" FontSize="10" Margin="3,0,0,0"
                                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                        </Button>
                                        <Button x:Name="BtnClearTrail" Style="{StaticResource ToolbarButtonStyle}"
                                                Padding="10,6"
                                                Click="BtnClearTrail_Click" ToolTip="Clear trail">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🗑" FontSize="14"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,0,0,10"/>

                                    <!-- 경로 설정 -->
                                    <TextBlock x:Name="SettingsTrailConfigLabel" Text="Trail Settings" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock x:Name="SettingsTrailColorLabel" Text="Trail Color" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" Margin="0,0,0,6"/>
                                        <StackPanel Orientation="Horizontal">
                                            <RadioButton x:Name="TrailColorBlue" GroupName="TrailColor"
                                                         Click="TrailColor_Changed" Tag="#2196F3" IsChecked="True" Margin="0,0,6,0">
                                                <Border Width="20" Height="20" CornerRadius="4" Background="#2196F3"/>
                                            </RadioButton>
                                            <RadioButton x:Name="TrailColorGreen" GroupName="TrailColor"
                                                         Click="TrailColor_Changed" Tag="#4CAF50" Margin="0,0,6,0">
                                                <Border Width="20" Height="20" CornerRadius="4" Background="#4CAF50"/>
                                            </RadioButton>
                                            <RadioButton x:Name="TrailColorRed" GroupName="TrailColor"
                                                         Click="TrailColor_Changed" Tag="#F44336" Margin="0,0,6,0">
                                                <Border Width="20" Height="20" CornerRadius="4" Background="#F44336"/>
                                            </RadioButton>
                                            <RadioButton x:Name="TrailColorYellow" GroupName="TrailColor"
                                                         Click="TrailColor_Changed" Tag="#FFEB3B" Margin="0,0,6,0">
                                                <Border Width="20" Height="20" CornerRadius="4" Background="#FFEB3B"/>
                                            </RadioButton>
                                            <RadioButton x:Name="TrailColorWhite" GroupName="TrailColor"
                                                         Click="TrailColor_Changed" Tag="#FFFFFF">
                                                <Border Width="20" Height="20" CornerRadius="4" Background="White"
                                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"/>
                                            </RadioButton>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,8">
                                        <TextBlock x:Name="SettingsTrailThicknessLabel" Text="Trail Thickness" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" Margin="0,0,0,6"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Slider x:Name="TrailThicknessSlider" Grid.Column="0" Minimum="1" Maximum="5" Value="2"
                                                    VerticalAlignment="Center" SmallChange="0.5" LargeChange="1"
                                                    ValueChanged="TrailThickness_Changed"/>
                                            <TextBlock x:Name="TrailThicknessText" Grid.Column="1" Text="2px" FontSize="11" Width="30"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       VerticalAlignment="Center" Margin="8,0,0,0" TextAlignment="Right"/>
                                        </Grid>
                                    </StackPanel>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,6,0,10"/>

                                    <!-- 자동화 설정 -->
                                    <TextBlock x:Name="SettingsAutomationLabel" Text="Automation" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="SettingsAutoTrackLabel" Grid.Column="0" Text="Auto-track on map load" Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11" VerticalAlignment="Center"/>
                                        <CheckBox x:Name="ChkAutoStartTracking" Grid.Column="1" IsChecked="False"
                                                  Checked="AutoStartTracking_Changed" Unchecked="AutoStartTracking_Changed"/>
                                    </Grid>
                                </StackPanel>

                                <!-- [단축키] 탭 -->
                                <StackPanel x:Name="PanelShortcuts" Visibility="Collapsed">
                                    <TextBlock x:Name="ShortcutsMapControlLabel" Text="Map Controls" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsZoomLabel" Grid.Column="0" Text="Zoom" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock x:Name="ShortcutsZoomKey" Grid.Column="1" Text="Scroll" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsPanLabel" Grid.Column="0" Text="Pan" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock x:Name="ShortcutsPanKey" Grid.Column="1" Text="Drag" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsResetLabel" Grid.Column="0" Text="Reset View" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock Grid.Column="1" Text="R" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,6,0,10"/>

                                    <TextBlock x:Name="ShortcutsLayerToggleLabel" Text="Layer Toggle" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsExtractLabel" Grid.Column="0" Text="Show/Hide Extracts" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock Grid.Column="1" Text="E / 1" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsTransitLabel" Grid.Column="0" Text="Show/Hide Transit" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock Grid.Column="1" Text="T / 2" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsQuestLabel" Grid.Column="0" Text="Show/Hide Quests" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock Grid.Column="1" Text="3" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>

                                    <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,6,0,10"/>

                                    <TextBlock x:Name="ShortcutsPanelLabel" Text="Panel" FontWeight="SemiBold" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsQuestPanelLabel" Grid.Column="0" Text="Quest Panel" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock Grid.Column="1" Text="Q" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ShortcutsFloorChangeLabel" Grid.Column="0" Text="Floor Change (Multi-floor)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        <TextBlock Grid.Column="1" Text="0~5" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                                   Background="{StaticResource BackgroundDarkBrush}" Padding="6,2"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>

                        <!-- Footer -->
                        <Border Grid.Row="3" Background="{StaticResource BackgroundLightBrush}"
                                CornerRadius="0,0,8,8" Padding="12,8">
                            <Grid>
                                <Button x:Name="BtnResetSettingsFooter" Content="Reset" Style="{StaticResource ToolbarButtonStyle}"
                                        HorizontalAlignment="Left" Padding="10,5" FontSize="10"
                                        Click="BtnResetSettings_Click"/>
                                <Button x:Name="BtnCloseSettingsFooter" Content="Close" Style="{StaticResource ToolbarButtonStyle}"
                                        HorizontalAlignment="Right" Padding="14,5" FontSize="11"
                                        Click="BtnCloseSettings_Click"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Status Bar - 최적화된 컴팩트 디자인 -->
        <Border Grid.Row="2" Background="{StaticResource BackgroundMediumBrush}"
                Padding="10,5" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 맵 이름 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="CurrentMapName" Text="Select Map" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="CurrentFloorName" Text="" FontSize="10"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center" Margin="6,0,0,0"/>
                </StackPanel>

                <!-- 마커 카운트 - 컴팩트 -->
                <Border Grid.Column="1" Background="{StaticResource BackgroundLightBrush}"
                        CornerRadius="4" Padding="8,3" Margin="12,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <!-- Extract -->
                        <StackPanel x:Name="ExtractCountPanel" Orientation="Horizontal" ToolTip="Extract">
                            <Border Width="6" Height="6" CornerRadius="1" Background="{StaticResource SuccessBrush}"
                                    VerticalAlignment="Center"/>
                            <TextBlock x:Name="MarkerCountText" Text="0" Foreground="{StaticResource SuccessBrush}"
                                       FontWeight="SemiBold" FontSize="10" Margin="3,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Rectangle Width="1" Fill="{StaticResource BorderBrush}" Margin="8,2"/>
                        <!-- Transit -->
                        <StackPanel x:Name="TransitCountPanel" Orientation="Horizontal" ToolTip="Transit Point">
                            <Border Width="6" Height="6" CornerRadius="1" Background="#FF9800"
                                    VerticalAlignment="Center"/>
                            <TextBlock x:Name="TransitCountText" Text="0" Foreground="#FF9800"
                                       FontWeight="SemiBold" FontSize="10" Margin="3,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Rectangle Width="1" Fill="{StaticResource BorderBrush}" Margin="8,2"/>
                        <!-- Quest -->
                        <StackPanel x:Name="QuestCountPanel" Orientation="Horizontal" ToolTip="Quest Objective">
                            <Border Width="6" Height="6" CornerRadius="3" Background="{StaticResource WarningBrush}"
                                    VerticalAlignment="Center"/>
                            <TextBlock x:Name="QuestMarkerCountText" Text="0" Foreground="{StaticResource WarningBrush}"
                                       FontWeight="SemiBold" FontSize="10" Margin="3,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 상태 텍스트 -->
                <TextBlock x:Name="StatusText" Grid.Column="2" Text="" Margin="12,0"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                <!-- 좌표 표시 -->
                <Border Grid.Column="3" Background="{StaticResource BackgroundLightBrush}" CornerRadius="4" Padding="8,3">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="GameCoordsText" Text="X: 0, Z: 0"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"
                                   FontFamily="Consolas" VerticalAlignment="Center"/>
                        <Button x:Name="BtnCopyCoords" Style="{StaticResource ToolbarButtonStyle}"
                                Content="📋" FontSize="10" Width="22" Height="20" Margin="6,0,0,0"
                                Click="BtnCopyCoords_Click"/>
                    </StackPanel>
                </Border>

                <!-- Tracking Status Indicator -->
                <StackPanel x:Name="TrackingStatusPanel" Grid.Column="4" Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center"
                            ToolTip="Tracking Status">
                    <Border x:Name="StatusTrackingIndicator" Width="8" Height="8" CornerRadius="4"
                            Background="Gray" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
