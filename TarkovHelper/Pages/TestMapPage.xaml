<UserControl x:Class="TarkovHelper.Pages.TestMapPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             xmlns:local="clr-namespace:TarkovHelper.Pages"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource BackgroundDarkBrush}"
             PreviewKeyDown="Page_PreviewKeyDown"
             Focusable="True">
    <UserControl.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:ProgressWidthConverter x:Key="ProgressWidthConverter"/>

        <!-- 툴바 버튼 스타일 -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource BackgroundDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 툴바 섹션 스타일 -->
        <Style x:Key="ToolbarSectionStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Toolbar - 개선된 디자인 -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}"
                Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.3" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Map Selector -->
                <Border Grid.Column="0" Style="{StaticResource ToolbarSectionStyle}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="맵" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="MapSelector" Width="160" Height="26"
                                  SelectionChanged="MapSelector_SelectionChanged"
                                  DisplayMemberPath="DisplayName"/>
                    </StackPanel>
                </Border>

                <!-- Floor Selector -->
                <Border x:Name="FloorSelectorPanel" Grid.Column="1" Style="{StaticResource ToolbarSectionStyle}"
                        Margin="8,0,0,0" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="층" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="FloorSelector" Width="100" Height="26"
                                  SelectionChanged="FloorSelector_SelectionChanged"/>
                        <TextBlock x:Name="TxtFloorHotkeys" Text="0-5" FontSize="9"
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="6,0,0,0"
                                   VerticalAlignment="Center" Opacity="0.6"/>
                    </StackPanel>
                </Border>

                <!-- Layer Visibility -->
                <Border Grid.Column="2" Style="{StaticResource ToolbarSectionStyle}" Margin="8,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox x:Name="ChkShowMarkers" IsChecked="True" VerticalAlignment="Center"
                                  Checked="LayerVisibility_Changed" Unchecked="LayerVisibility_Changed"
                                  ToolTip="탈출구 표시/숨김 (1 또는 E)">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="2" Background="{StaticResource SuccessBrush}" Margin="0,0,5,0"/>
                                <TextBlock Text="탈출" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkShowTransit" IsChecked="True" VerticalAlignment="Center" Margin="12,0,0,0"
                                  Checked="LayerVisibility_Changed" Unchecked="LayerVisibility_Changed"
                                  ToolTip="환승 지점 표시/숨김 (2 또는 T)">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="2" Background="#FF9800" Margin="0,0,5,0"/>
                                <TextBlock Text="환승" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                            </StackPanel>
                        </CheckBox>
                        <CheckBox x:Name="ChkShowQuests" IsChecked="True" VerticalAlignment="Center" Margin="12,0,0,0"
                                  Checked="LayerVisibility_Changed" Unchecked="LayerVisibility_Changed"
                                  ToolTip="퀘스트 목표 표시/숨김 (3)">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="5" Background="{StaticResource WarningBrush}" Margin="0,0,5,0"/>
                                <TextBlock Text="퀘스트" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                            </StackPanel>
                        </CheckBox>
                    </StackPanel>
                </Border>

                <!-- Tracker Controls -->
                <Border Grid.Column="3" Style="{StaticResource ToolbarSectionStyle}" Margin="8,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="BtnSelectFolder" Style="{StaticResource ToolbarButtonStyle}"
                                Content="📁" Width="28" Height="26" FontSize="14"
                                Click="BtnSelectFolder_Click" ToolTip="스크린샷 폴더 선택"/>
                        <Button x:Name="BtnToggleTracking" Style="{StaticResource ToolbarButtonStyle}"
                                Content="▶" Width="28" Height="26" FontSize="12" Margin="4,0,0,0"
                                Click="BtnToggleTracking_Click" ToolTip="트래킹 시작/중지"/>
                        <Button x:Name="BtnClearTrail" Style="{StaticResource ToolbarButtonStyle}"
                                Content="🗑" Width="28" Height="26" FontSize="12" Margin="4,0,0,0"
                                Click="BtnClearTrail_Click" ToolTip="경로 초기화"/>
                        <Border x:Name="TrackingStatusBorder" Width="10" Height="10" CornerRadius="5"
                                Background="Gray" Margin="8,0,0,0" VerticalAlignment="Center"
                                ToolTip="트래킹 상태"/>
                    </StackPanel>
                </Border>

                <!-- Spacer -->
                <TextBlock x:Name="TxtFloorLabel" Grid.Column="4" Visibility="Collapsed"/>

                <!-- Help/Legend Button -->
                <Border Grid.Column="5" Style="{StaticResource ToolbarSectionStyle}" Margin="0,0,8,0">
                    <Button x:Name="BtnLegend" Style="{StaticResource ToolbarButtonStyle}"
                            Width="28" Height="26" FontSize="14" Content="?"
                            Click="BtnLegend_Click" ToolTip="맵 범례 보기"/>
                </Border>

                <!-- Zoom Controls -->
                <Border Grid.Column="6" Style="{StaticResource ToolbarSectionStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="BtnZoomOut" Style="{StaticResource ToolbarButtonStyle}"
                                Content="−" Width="28" Height="26" FontSize="16" FontWeight="Bold"
                                Click="BtnZoomOut_Click" ToolTip="축소 (Scroll Down)"/>
                        <Border Background="{StaticResource BackgroundDarkBrush}" CornerRadius="4" Margin="6,0" Padding="10,4">
                            <TextBlock x:Name="ZoomText" Text="100%" Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"
                                       Width="40" TextAlignment="Center"/>
                        </Border>
                        <Button x:Name="BtnZoomIn" Style="{StaticResource ToolbarButtonStyle}"
                                Content="+" Width="28" Height="26" FontSize="16" FontWeight="Bold"
                                Click="BtnZoomIn_Click" ToolTip="확대 (Scroll Up)"/>
                        <Rectangle Width="1" Fill="{StaticResource BorderBrush}" Margin="8,4"/>
                        <Button x:Name="BtnResetView" Style="{StaticResource ToolbarButtonStyle}"
                                Content="⟲" Width="28" Height="26" FontSize="14"
                                Click="BtnResetView_Click" ToolTip="뷰 초기화 (R)"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content with Drawer -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="QuestDrawerColumn" Width="0"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Quest Drawer (Left) -->
            <Border x:Name="QuestDrawerPanel" Grid.Column="0"
                    Background="{StaticResource BackgroundMediumBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,1,0"
                    Visibility="Collapsed"
                    KeyDown="QuestDrawer_KeyDown" Focusable="True">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.4" Direction="0"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 0: Header -->
                    <Border Grid.Row="0" Padding="12,10" Background="{StaticResource BackgroundLightBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="퀘스트 목록" FontWeight="SemiBold" FontSize="13"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                                <TextBlock x:Name="QuestCountText" Text="(0)" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}" Margin="8,0,0,0"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button Content="✕" HorizontalAlignment="Right" Width="24" Height="24"
                                    Style="{StaticResource ToolbarButtonStyle}"
                                    Click="BtnCloseDrawer_Click" FontSize="11"/>
                        </Grid>
                    </Border>

                    <!-- Row 1: Search with Watermark -->
                    <Border Grid.Row="1" Padding="10,8" Background="{StaticResource BackgroundMediumBrush}">
                        <Grid>
                            <TextBox x:Name="SearchBox" Background="{StaticResource BackgroundLightBrush}"
                                     Foreground="{StaticResource TextPrimaryBrush}"
                                     BorderBrush="{StaticResource BorderBrush}"
                                     Padding="8,6" FontSize="11" TextChanged="SearchBox_TextChanged"/>
                            <TextBlock Text="퀘스트 또는 목표 검색..." FontSize="11"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Padding="10,7" IsHitTestVisible="False"
                                       Visibility="{Binding Text, ElementName=SearchBox, Converter={StaticResource StringToVisibilityConverter}}"/>
                        </Grid>
                    </Border>

                    <!-- Row 2: Filters & Sort -->
                    <Border Grid.Row="2" Padding="10,6" Background="{StaticResource BackgroundMediumBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <CheckBox x:Name="ChkIncompleteOnly" Content="미완료만"
                                          Foreground="{StaticResource TextSecondaryBrush}" FontSize="10"
                                          Checked="DrawerFilter_Changed" Unchecked="DrawerFilter_Changed"/>
                                <CheckBox x:Name="ChkCurrentMapOnly" Content="현재 맵" IsChecked="True"
                                          Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" Margin="10,0,0,0"
                                          Checked="DrawerFilter_Changed" Unchecked="DrawerFilter_Changed"/>
                            </StackPanel>
                            <ComboBox Grid.Column="1" Width="75" FontSize="10"
                                      SelectionChanged="SortOption_Changed" SelectedIndex="0">
                                <ComboBoxItem Content="이름순" Tag="name"/>
                                <ComboBoxItem Content="진행률" Tag="progress"/>
                                <ComboBoxItem Content="개수순" Tag="count"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- Row 3: Quick Actions -->
                    <Border Grid.Row="3" Padding="10,6" Background="{StaticResource BackgroundMediumBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Style="{StaticResource ToolbarButtonStyle}" FontSize="9" Padding="8,4" Margin="2,0"
                                    Click="BtnShowAll_Click" ToolTip="모든 퀘스트 표시">
                                <TextBlock Text="전체 표시"/>
                            </Button>
                            <Button Style="{StaticResource ToolbarButtonStyle}" FontSize="9" Padding="8,4" Margin="2,0"
                                    Click="BtnHideAll_Click" ToolTip="모든 퀘스트 숨김">
                                <TextBlock Text="전체 숨김"/>
                            </Button>
                            <Button Style="{StaticResource ToolbarButtonStyle}" FontSize="9" Padding="8,4" Margin="2,0"
                                    Click="BtnExpandAll_Click" ToolTip="모두 펼치기">
                                <TextBlock Text="펼치기"/>
                            </Button>
                            <Button Style="{StaticResource ToolbarButtonStyle}" FontSize="9" Padding="8,4" Margin="2,0"
                                    Click="BtnCollapseAll_Click" ToolTip="모두 접기">
                                <TextBlock Text="접기"/>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Row 4: Quest List -->
                    <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <!-- 빈 상태 메시지 -->
                            <StackPanel x:Name="EmptyStatePanel" Visibility="Collapsed"
                                        VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
                                <TextBlock Text="📋" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,12" Opacity="0.5"/>
                                <TextBlock Text="표시할 퀘스트가 없습니다" FontSize="13"
                                           Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Text="필터를 조정하거나 다른 맵을 선택하세요" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"
                                           Margin="0,6,0,0" Opacity="0.7"/>
                            </StackPanel>

                            <!-- 퀘스트 목록 -->
                            <ItemsControl x:Name="QuestObjectivesList" Margin="0,4">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <!-- 퀘스트 헤더 -->
                                        <Border Padding="10,8" Cursor="Hand" MouseLeftButtonDown="QuestHeader_Click">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                                            <Setter Property="Background" Value="#3D3D20"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#353540"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Border.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="모두 완료로 표시" Click="QuestContextMenu_MarkAllComplete"/>
                                                    <MenuItem Header="모두 미완료로 표시" Click="QuestContextMenu_MarkAllIncomplete"/>
                                                </ContextMenu>
                                            </Border.ContextMenu>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- 접기/펼치기 아이콘 -->
                                                <TextBlock Grid.Row="0" Grid.Column="0" FontSize="8"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           VerticalAlignment="Center" Margin="0,0,6,0" Width="10">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="▼"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                                                                    <Setter Property="Text" Value="▶"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <!-- 맵 표시 토글 -->
                                                <CheckBox Grid.Row="0" Grid.Column="1" IsChecked="{Binding IsVisible, Mode=OneWay}"
                                                          VerticalAlignment="Center" Margin="0,0,6,0"
                                                          Click="QuestVisibilityToggle_Click" ToolTip="맵에 표시/숨김"/>

                                                <!-- 상태 표시 -->
                                                <Ellipse Grid.Row="0" Grid.Column="2" Width="8" Height="8"
                                                         VerticalAlignment="Center" Margin="0,0,8,0">
                                                    <Ellipse.Style>
                                                        <Style TargetType="Ellipse">
                                                            <Setter Property="Fill" Value="{StaticResource WarningBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                    <Setter Property="Fill" Value="{StaticResource SuccessBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Ellipse.Style>
                                                </Ellipse>

                                                <!-- 퀘스트 이름 -->
                                                <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding QuestName}" FontSize="12"
                                                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                                    <Setter Property="Opacity" Value="0.6"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <!-- 진행률 텍스트 -->
                                                <TextBlock Grid.Row="0" Grid.Column="4" Text="{Binding ProgressText}" FontSize="10"
                                                           VerticalAlignment="Center" Margin="8,0,0,0">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>

                                                <!-- 진행률 바 (Row 1) -->
                                                <Grid Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="3" Height="3" Margin="0,6,0,0">
                                                    <Border Background="{StaticResource BackgroundDarkBrush}" CornerRadius="1.5"/>
                                                    <Border CornerRadius="1.5" HorizontalAlignment="Left"
                                                            Width="{Binding ProgressPercent, Converter={StaticResource ProgressWidthConverter}}">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                    </Border>
                                                </Grid>
                                            </Grid>
                                        </Border>

                                        <!-- 목표 리스트 (접기/펼치기) -->
                                        <ItemsControl ItemsSource="{Binding Objectives}">
                                            <ItemsControl.Style>
                                                <Style TargetType="ItemsControl">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExpanded}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ItemsControl.Style>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Padding="10,6" Cursor="Hand"
                                                            MouseLeftButtonDown="QuestDrawerItem_Click"
                                                            MouseRightButtonDown="QuestDrawerItem_RightClick"
                                                            MouseEnter="QuestDrawerItem_MouseEnter"
                                                            MouseLeave="QuestDrawerItem_MouseLeave"
                                                            ToolTip="클릭: 맵 이동 | 우클릭: 완료 토글">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <Grid Margin="24,0,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <!-- 완료 체크 -->
                                                            <TextBlock Grid.Column="0" Text="✓" FontSize="10" Margin="0,0,6,0" VerticalAlignment="Center">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="Transparent"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>

                                                            <!-- 타입 아이콘 -->
                                                            <TextBlock Grid.Column="1" Text="{Binding TypeIcon}" FontSize="11"
                                                                       Margin="0,0,6,0" VerticalAlignment="Center" ToolTip="{Binding Objective.Type}"/>

                                                            <!-- 설명 -->
                                                            <TextBlock Grid.Column="2" Text="{Binding DescriptionDisplay}" FontSize="11"
                                                                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                                                <Setter Property="Opacity" Value="0.5"/>
                                                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>

                                                            <!-- 맵 태그 -->
                                                            <Border Grid.Column="3" Margin="6,0,0,0" Padding="5,2" CornerRadius="3"
                                                                    Background="{StaticResource BackgroundDarkBrush}" VerticalAlignment="Center"
                                                                    Visibility="{Binding ShowMapTag, Converter={StaticResource BoolToVisibilityConverter}}"
                                                                    ToolTip="{Binding MapName}">
                                                                <TextBlock Text="{Binding MapTag}" FontSize="9"
                                                                           Foreground="{StaticResource TextSecondaryBrush}"/>
                                                            </Border>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        </Grid>
                    </ScrollViewer>

                    <!-- Row 5: Keyboard Hints -->
                    <Border Grid.Row="5" Background="{StaticResource BackgroundDarkBrush}" Padding="10,8"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" Opacity="0.8">
                                <Run Text="Q" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                <Run Text=" 열기/닫기"/>
                            </TextBlock>
                            <TextBlock FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" Margin="14,0,0,0" Opacity="0.8">
                                <Run Text="↑↓" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                <Run Text=" 이동"/>
                            </TextBlock>
                            <TextBlock FontSize="9" Foreground="{StaticResource TextSecondaryBrush}" Margin="14,0,0,0" Opacity="0.8">
                                <Run Text="Enter" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                <Run Text=" 포커스"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Resize Grip -->
                    <Border Grid.RowSpan="6" Width="5" HorizontalAlignment="Right" Background="Transparent"
                            Cursor="SizeWE" MouseLeftButtonDown="DrawerResizeGrip_MouseDown"
                            MouseMove="DrawerResizeGrip_MouseMove" MouseLeftButtonUp="DrawerResizeGrip_MouseUp">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                        <Setter Property="Opacity" Value="0.5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>
                </Grid>
            </Border>

            <!-- Drawer Toggle Button -->
            <Border Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0"
                    Panel.ZIndex="10" CornerRadius="0,6,6,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="2" Opacity="0.4" Direction="0"/>
                </Border.Effect>
                <Button x:Name="BtnToggleDrawer" Width="28" Height="90"
                        Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,1,1"
                        Click="BtnToggleDrawer_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="0,6,6,0">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock x:Name="arrow" Text="◀" FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Text="Q" FontSize="9" Margin="0,4,0,0"
                                               Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                    <Setter TargetName="arrow" Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Border>

            <!-- Map Viewer -->
            <Grid Grid.Column="1" x:Name="MapViewerGrid" Background="{StaticResource BackgroundDarkBrush}"
                  ClipToBounds="True"
                  MouseLeftButtonDown="MapViewer_MouseLeftButtonDown"
                  MouseLeftButtonUp="MapViewer_MouseLeftButtonUp"
                  MouseMove="MapViewer_MouseMove"
                  MouseWheel="MapViewer_MouseWheel">

                <Canvas x:Name="MapCanvas" RenderTransformOrigin="0,0">
                    <Canvas.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="MapScale" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="MapTranslate" X="0" Y="0"/>
                        </TransformGroup>
                    </Canvas.RenderTransform>

                    <!-- SVG Map Background -->
                    <svgc:SvgViewbox x:Name="MapSvg" Stretch="None" Canvas.Left="0" Canvas.Top="0"/>

                    <!-- Markers Layer -->
                    <Canvas x:Name="MarkersCanvas"/>

                    <!-- Transit Layer -->
                    <Canvas x:Name="TransitCanvas"/>

                    <!-- Quest Markers Layer -->
                    <Canvas x:Name="QuestMarkersCanvas"/>

                    <!-- Trail Layer (player movement path) -->
                    <Canvas x:Name="TrailCanvas"/>

                    <!-- Player Position Marker -->
                    <Canvas x:Name="PlayerMarkerCanvas"/>
                </Canvas>

                <!-- Loading Indicator -->
                <Border x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Width="40" Height="40" CornerRadius="20" Background="{StaticResource AccentBrush}">
                            <Border.RenderTransform>
                                <RotateTransform x:Name="LoadingRotation" CenterX="20" CenterY="20"/>
                            </Border.RenderTransform>
                            <TextBlock Text="⟳" FontSize="24" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="맵 로딩 중..." FontSize="12" Margin="0,12,0,0"
                                   Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Legend Popup -->
                <Border x:Name="LegendPopup" Visibility="Collapsed"
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Margin="0,10,10,0" Background="{StaticResource BackgroundMediumBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="6" Padding="14">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="맵 범례" FontWeight="SemiBold" FontSize="13"
                                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Width="14" Height="14" CornerRadius="3" Background="{StaticResource SuccessBrush}"/>
                            <TextBlock Text="탈출구" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Width="14" Height="14" CornerRadius="3" Background="#FF9800"/>
                            <TextBlock Text="환승 지점" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <Border Width="14" Height="14" CornerRadius="7" Background="{StaticResource WarningBrush}"/>
                            <TextBlock Text="퀘스트 목표" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,10"/>

                        <TextBlock Text="퀘스트 타입" FontWeight="SemiBold" FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="📍" FontSize="12" Width="20"/>
                            <TextBlock Text="방문" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="🎯" FontSize="12" Width="20"/>
                            <TextBlock Text="마킹" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="📦" FontSize="12" Width="20"/>
                            <TextBlock Text="아이템 설치" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="🚪" FontSize="12" Width="20"/>
                            <TextBlock Text="탈출" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,3">
                            <TextBlock Text="💀" FontSize="12" Width="20"/>
                            <TextBlock Text="처치" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Button Content="닫기" Margin="0,12,0,0" Padding="12,6" FontSize="10"
                                Style="{StaticResource ToolbarButtonStyle}" Click="BtnCloseLegend_Click"/>
                    </StackPanel>
                </Border>

                <!-- Keyboard Shortcuts Hint (맵 영역) -->
                <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="40,0,0,10"
                        Background="#90000000" CornerRadius="4" Padding="10,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}">
                            <Run Text="스크롤" Foreground="{StaticResource AccentBrush}"/>
                            <Run Text=" 줌"/>
                        </TextBlock>
                        <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0">
                            <Run Text="드래그" Foreground="{StaticResource AccentBrush}"/>
                            <Run Text=" 이동"/>
                        </TextBlock>
                        <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0">
                            <Run Text="R" Foreground="{StaticResource AccentBrush}"/>
                            <Run Text=" 리셋"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource BackgroundMediumBrush}"
                Padding="12,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 맵 이름 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="CurrentMapName" Text="맵 선택" FontSize="12" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="CurrentFloorName" Text="" FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- 상태 텍스트 -->
                <TextBlock x:Name="StatusText" Grid.Column="1" Text="" Margin="20,0"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                <!-- 마커 카운트 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,16,0">
                    <Border Background="{StaticResource BackgroundLightBrush}" CornerRadius="4" Padding="8,4" Margin="0,0,6,0">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="8" Height="8" CornerRadius="2" Background="{StaticResource SuccessBrush}"
                                    Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="MarkerCountText" Text="0" Foreground="{StaticResource SuccessBrush}"
                                       FontWeight="SemiBold" FontSize="11"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{StaticResource BackgroundLightBrush}" CornerRadius="4" Padding="8,4" Margin="0,0,6,0">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="8" Height="8" CornerRadius="2" Background="#2196F3"
                                    Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TransitCountText" Text="0" Foreground="#2196F3"
                                       FontWeight="SemiBold" FontSize="11"/>
                        </StackPanel>
                    </Border>
                    <Border Background="{StaticResource BackgroundLightBrush}" CornerRadius="4" Padding="8,4">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="8" Height="8" CornerRadius="4" Background="{StaticResource WarningBrush}"
                                    Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="QuestMarkerCountText" Text="0" Foreground="{StaticResource WarningBrush}"
                                       FontWeight="SemiBold" FontSize="11"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- 좌표 표시 + 복사 버튼 -->
                <Border Grid.Column="3" Background="{StaticResource BackgroundLightBrush}" CornerRadius="4" Padding="10,4">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="GameCoordsText" Text="X: 0, Z: 0"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"
                                   FontFamily="Consolas" Width="120" VerticalAlignment="Center"/>
                        <Button x:Name="BtnCopyCoords" Style="{StaticResource ToolbarButtonStyle}"
                                Content="📋" FontSize="11" Width="24" Height="22" Margin="6,0,0,0"
                                Click="BtnCopyCoords_Click" ToolTip="좌표 복사"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
