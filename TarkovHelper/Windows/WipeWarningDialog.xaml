<Window x:Class="TarkovHelper.Windows.WipeWarningDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wipe Warning"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False">

    <Border Background="{DynamicResource BackgroundDarkBrush}"
            CornerRadius="8" Padding="24"
            MinWidth="500" MaxWidth="600">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="" FontSize="{DynamicResource FontSizeHeader}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock x:Name="TxtTitle" Text="Before Quest Sync" FontSize="{DynamicResource FontSizeXLarge}" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button x:Name="BtnClose" Content="X"
                        HorizontalAlignment="Right"
                        Padding="8,4" FontSize="{DynamicResource FontSizeSmall}"
                        Click="BtnClose_Click"/>
            </Grid>

            <!-- Warning Message -->
            <TextBlock x:Name="TxtMessage"
                       Text="Have you recently reset your account (wipe)?"
                       FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12"/>

            <TextBlock x:Name="TxtDescription"
                       Text="If you sync after a wipe, logs from the previous season may mix and quest progress may be displayed incorrectly."
                       FontSize="{DynamicResource FontSizeXSmall}"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16"/>

            <!-- Log Folder Path -->
            <Border Background="{DynamicResource BackgroundMediumBrush}"
                    CornerRadius="4" Padding="12" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock x:Name="TxtLogFolderLabel" Text="Log folder location:"
                               FontSize="{DynamicResource FontSizeXSmall}" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               Margin="0,0,0,4"/>
                    <TextBlock x:Name="TxtLogPath"
                               Text=""
                               FontSize="{DynamicResource FontSizeTiny}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>

            <!-- Recommendation -->
            <TextBlock x:Name="TxtRecommendation"
                       Text="Recommended: Delete or backup log folders dated before the wipe."
                       FontSize="{DynamicResource FontSizeXSmall}"
                       Foreground="{DynamicResource AccentBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16"/>

            <!-- Don't show again checkbox -->
            <CheckBox x:Name="ChkDontShowAgain"
                      Content="Don't show this again"
                      Foreground="{DynamicResource TextSecondaryBrush}"
                      Margin="0,0,0,16"/>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnOpenFolder" Content="Open Folder"
                        Padding="16,8" Margin="0,0,8,0"
                        Click="BtnOpenFolder_Click"/>
                <Button x:Name="BtnContinue" Content="Continue"
                        Padding="16,8"
                        Click="BtnContinue_Click"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="{DynamicResource BackgroundDarkBrush}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
