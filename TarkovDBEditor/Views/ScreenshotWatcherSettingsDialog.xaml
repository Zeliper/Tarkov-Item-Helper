<Window x:Class="TarkovDBEditor.Views.ScreenshotWatcherSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screenshot Watcher Settings"
        Height="300" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Screenshot Folder -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Screenshot Folder:" VerticalAlignment="Center"/>
            <TextBox x:Name="FolderPathTextBox" Grid.Column="1" VerticalAlignment="Center"/>
            <Button x:Name="BrowseButton" Grid.Column="2" Content="Browse..." Click="BrowseButton_Click" Padding="10,6"/>
            <Button x:Name="AutoDetectButton" Grid.Column="3" Content="Auto-Detect" Click="AutoDetectButton_Click" Padding="10,6"/>
        </Grid>

        <!-- Regex Pattern -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Regex Pattern:" VerticalAlignment="Center"/>
            <TextBox x:Name="PatternTextBox" Grid.Column="1" VerticalAlignment="Center" FontFamily="Consolas" FontSize="11"/>
            <Button x:Name="ResetPatternButton" Grid.Column="2" Content="Reset" Click="ResetPatternButton_Click" Padding="10,6"/>
        </Grid>

        <!-- Current Status -->
        <Border Grid.Row="2" Background="#252526" BorderBrush="#3F3F46" BorderThickness="1" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock x:Name="WatcherStatusText" Text="Watcher Status: Not running" FontWeight="Bold"/>
                <TextBlock x:Name="LastPositionText" Text="Last Position: --" Margin="0,5,0,0" Foreground="#70A800"/>
            </StackPanel>
        </Border>

        <!-- Watcher Control Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Top">
            <Button x:Name="StartWatcherButton" Content="Start Watcher" Click="StartWatcherButton_Click"
                    Background="#0078D4"/>
            <Button x:Name="StopWatcherButton" Content="Stop Watcher" Click="StopWatcherButton_Click"
                    Background="#D41C00"/>
        </StackPanel>

        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click"
                    Background="#0078D4"/>
            <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
